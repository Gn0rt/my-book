1. Vue.js là gì và tại sao bạn nên học?

Vue.js là framework JavaScript được tạo ra bởi Evan You – một cựu kỹ sư của Google, người từng làm việc với Angular. Ông muốn tạo ra một framework nhẹ hơn, dễ học hơn nhưng vẫn mạnh mẽ để xây dựng giao diện web hiện đại.

Vue.js giúp lập trình viên:

Xây dựng ứng dụng web động nhanh chóng.

Dễ dàng quản lý dữ liệu và giao diện (data-binding).

Tái sử dụng các component nhỏ, giúp code gọn gàng và dễ bảo trì.

Điểm đặc biệt của Vue.js là cực kỳ thân thiện với người mới học. Bạn có thể thêm Vue chỉ bằng một thẻ <script> trong HTML, và ngay lập tức viết code tương tác mà không cần cấu hình phức tạp.

2. Cài đặt Vue – Bắt đầu cực nhanh ⚡

Có hai cách chính để bắt đầu:

🧩 Cách 1: Dùng trực tiếp qua CDN (dành cho người mới)
<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <title>Vue Cơ Bản</title>
  <script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
</head>
<body>
  <div id="app">
    <h1>Xin chào {{ name }}!</h1>
    <input v-model="name" placeholder="Nhập tên của bạn" />
  </div>

  <script>
    const { createApp } = Vue
    createApp({
      data() {
        return { name: 'Vue.js Developer' }
      }
    }).mount('#app')
  </script>
</body>
</html>


👉 Kết quả: khi bạn nhập tên vào ô input, phần chào ở trên sẽ tự động cập nhật theo — đó chính là reactivity của Vue.

🧠 Cách 2: Tạo project chuyên nghiệp (Vue CLI / Vite)

Nếu bạn muốn tạo dự án thực tế:

# Cài đặt Vite (nhanh hơn nhiều so với Vue CLI)
npm create vite@latest my-vue-app -- --template vue
cd my-vue-app
npm install
npm run dev


Và thế là bạn đã có môi trường Vue hiện đại sẵn sàng!

3. Cấu trúc dự án Vue 🏗️

Sau khi tạo xong, bạn sẽ thấy cấu trúc như sau:

my-vue-app/
├── src/
│   ├── components/
│   │   └── HelloWorld.vue
│   ├── App.vue
│   └── main.js
├── index.html
└── package.json


main.js: file chính, nơi Vue được khởi tạo.

App.vue: component gốc (root component).

components/: chứa các component con (ví dụ: button, card, header...).

4. Tìm hiểu về Component trong Vue ⚙️

Vue hoạt động dựa trên component — những mảnh nhỏ có thể tái sử dụng để tạo nên giao diện.

Ví dụ: tạo một component UserCard.vue đơn giản:

<template>
  <div class="card">
    <h3>{{ user.name }}</h3>
    <p>Tuổi: {{ user.age }}</p>
  </div>
</template>

<script>
export default {
  props: ['user']
}
</script>

<style scoped>
.card {
  border: 1px solid #ccc;
  padding: 12px;
  border-radius: 8px;
}
</style>


Rồi trong App.vue, bạn có thể sử dụng nó như thế này:

<template>
  <div>
    <UserCard :user="{ name: 'Bình', age: 22 }" />
    <UserCard :user="{ name: 'Trang', age: 25 }" />
  </div>
</template>

<script>
import UserCard from './components/UserCard.vue'

export default {
  components: { UserCard }
}
</script>


Và chỉ như vậy thôi, bạn đã hiểu cách component hoạt động trong Vue.

5. Reactive Data – Linh hồn của Vue 💫

Trong Vue, mọi thứ trong data() đều “sống”. Khi bạn thay đổi dữ liệu, giao diện tự động cập nhật mà không cần DOM manipulation.

Ví dụ:

<template>
  <div>
    <h1>{{ counter }}</h1>
    <button @click="counter++">Tăng</button>
  </div>
</template>

<script>
export default {
  data() {
    return { counter: 0 }
  }
}
</script>


Không cần document.getElementById hay gì cả — Vue tự xử lý tất cả!

6. v-if, v-for, v-model – Bộ ba quyền lực 🔥
✅ v-if

Dùng để hiển thị hoặc ẩn phần tử:

<p v-if="isLogin">Chào mừng bạn quay lại!</p>
<p v-else>Vui lòng đăng nhập</p>

🔁 v-for

Lặp qua danh sách:

<li v-for="(book, index) in books" :key="index">
  {{ index + 1 }}. {{ book }}
</li>

💬 v-model

Kết nối dữ liệu hai chiều giữa input và biến:

<input v-model="email" placeholder="Nhập email">
<p>Email của bạn: {{ email }}</p>

7. Computed & Watcher – “Trí tuệ” trong Vue 🧠
🧮 Computed

Dùng khi bạn cần biến phụ thuộc vào dữ liệu khác:

<template>
  <p>Tổng: {{ total }}</p>
</template>

<script>
export default {
  data() {
    return { a: 5, b: 10 }
  },
  computed: {
    total() {
      return this.a + this.b
    }
  }
}
</script>

👀 Watcher

Theo dõi sự thay đổi của biến:

watch: {
  counter(newVal, oldVal) {
    console.log(`Counter thay đổi từ ${oldVal} -> ${newVal}`)
  }
}

8. Lifecycle Hooks – Vòng đời của component ⏳

Mỗi component Vue có một vòng đời rõ ràng:

created() – gọi khi component được khởi tạo.

mounted() – gọi khi DOM đã sẵn sàng.

updated() – gọi khi dữ liệu thay đổi.

unmounted() – gọi khi component bị xóa.

Ví dụ:

mounted() {
  console.log('Component đã hiển thị trên giao diện!')
}

9. Giao tiếp giữa các component 🔗

Vue cho phép truyền dữ liệu qua lại giữa cha và con.

🟩 Từ cha → con:

Dùng props:

<ChildComponent :title="pageTitle" />

🟦 Từ con → cha:

Dùng emit event:

<!-- Child.vue -->
<button @click="$emit('increment')">Tăng</button>

<!-- Parent.vue -->
<Child @increment="count++" />

10. Kết hợp với Pinia hoặc Vuex để quản lý trạng thái 🗃️

Khi dự án lớn lên, bạn cần một store để quản lý dữ liệu toàn cục.
Hiện nay Vue 3 khuyến khích dùng Pinia (nhẹ và dễ dùng hơn Vuex).

Cài đặt:

npm install pinia


Sử dụng:

// store/counter.js
import { defineStore } from 'pinia'

export const useCounterStore = defineStore('counter', {
  state: () => ({ count: 0 }),
  actions: {
    increment() {
      this.count++
    }
  }
})


Trong component:

import { useCounterStore } from './store/counter'

const store = useCounterStore()
store.increment()
console.log(store.count)

11. Lời khuyên cho người mới học Vue 🌱

Bắt đầu bằng HTML + CDN trước khi vào dự án thực tế.

Làm các mini project như: To-do List, Calculator, Weather App.

Dần làm quen với Composition API, Pinia, và Routing.

Đọc tài liệu chính thức tại vuejs.org
.

Luôn học thông qua dự án nhỏ – đừng chỉ đọc lý thuyết.

🏁 Kết luận

Vue.js là lựa chọn hoàn hảo cho người mới bước vào thế giới front-end.
Nó dễ hiểu, mạnh mẽ, và cực kỳ linh hoạt. Bạn có thể học nhanh, tạo ra ứng dụng thực tế, và dùng nó để xin việc hay làm sản phẩm cá nhân.

“Nếu React là một cỗ máy mạnh mẽ, Angular là doanh nghiệp nghiêm túc, thì Vue chính là người bạn sẵn sàng giúp bạn bắt đầu hành trình lập trình front-end mà không hề thấy sợ.”