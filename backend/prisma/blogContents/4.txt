1. Mở đầu — Vì sao nên học TypeScript?

Nếu bạn từng viết JavaScript và gặp lỗi kiểu như:

const user = { name: "Bình" };
console.log(user.age.toFixed(2)); // 💥 Lỗi runtime!


Thì xin chúc mừng — bạn chính là lý do TypeScript ra đời 😄

TypeScript (TS) là phiên bản nâng cấp của JavaScript, do Microsoft phát triển, nhằm giúp code an toàn, dễ đọc và dễ bảo trì hơn.

Nói đơn giản:

TypeScript = JavaScript + Kiểu dữ liệu (Types)

Nó không thay thế JS, mà mở rộng nó. Bất kỳ code TypeScript nào cuối cùng cũng biên dịch về JavaScript để chạy trên trình duyệt hoặc Node.js.

2. TypeScript khác gì với JavaScript?
JavaScript	TypeScript
Ngôn ngữ động (dynamic typing)	Ngôn ngữ tĩnh (static typing)
Phát hiện lỗi khi chạy	Phát hiện lỗi khi biên dịch
Không có kiểu dữ liệu cụ thể	Có thể định nghĩa kiểu cho mọi thứ
Dễ bắt đầu nhưng khó bảo trì	Học thêm chút nhưng code an toàn hơn

Một ví dụ dễ hiểu:

// JavaScript
function add(a, b) {
  return a + b;
}

console.log(add("5", 10)); // Kết quả: "510" (vì string + number)


Trong TypeScript:

function add(a: number, b: number): number {
  return a + b;
}

console.log(add("5", 10)); // ❌ Lỗi: Argument of type 'string' is not assignable to parameter of type 'number'.


Nhờ có type checking, TS giúp bạn tránh lỗi trước khi code được chạy.

3. Cài đặt TypeScript ⚙️

Cách đơn giản nhất để bắt đầu là dùng npm:

npm install -g typescript


Kiểm tra phiên bản:

tsc -v


Tạo file hello.ts:

const message: string = "Xin chào TypeScript!";
console.log(message);


Biên dịch sang JavaScript:

tsc hello.ts


Kết quả là file hello.js mà bạn có thể chạy bằng Node.js:

node hello.js

4. Khai báo kiểu dữ liệu cơ bản 🧩

TypeScript có các kiểu dữ liệu quen thuộc:

let age: number = 22;
let name: string = "Minh";
let isOnline: boolean = true;
let skills: string[] = ["HTML", "CSS", "Vue"];
let anything: any = 123; // "any" cho phép mọi loại giá trị


Đặc biệt, nếu bạn không khai báo kiểu, TS sẽ tự suy luận (type inference):

let count = 5; // TS hiểu là number
count = "5";  // ❌ Lỗi: không thể gán string cho number

5. Type Alias & Union Type 💡

Khi muốn tái sử dụng kiểu dữ liệu, ta có thể đặt tên cho chúng:

type UserID = number | string;

let id1: UserID = 123;
let id2: UserID = "ABC123";


Union type (|) cho phép một biến có nhiều kiểu dữ liệu có thể chấp nhận được.

6. Interface – Khung xương cho đối tượng 🧱

Interface giúp bạn định nghĩa cấu trúc của một object.

interface User {
  name: string;
  age: number;
  email?: string; // dấu ? nghĩa là tùy chọn
}

const user: User = {
  name: "Hà",
  age: 21,
};


Nếu bạn cố gắng gán sai kiểu, TypeScript sẽ cảnh báo:

user.age = "hai mươi mốt"; // ❌ Sai kiểu

7. Function trong TypeScript 🧠

Khi viết hàm, bạn có thể khai báo kiểu cho tham số và giá trị trả về:

function greet(name: string): string {
  return `Xin chào, ${name}!`;
}


Hoặc dùng arrow function:

const sum = (a: number, b: number): number => a + b;


Bạn cũng có thể định nghĩa tham số mặc định hoặc tùy chọn:

function showInfo(name: string, age?: number) {
  console.log(`Tên: ${name}`);
  if (age) console.log(`Tuổi: ${age}`);
}

8. Enum – Đại diện cho các giá trị cố định 🔢

Enum là cách tốt để nhóm các giá trị có liên quan.

enum Role {
  User,
  Admin,
  Guest
}

let currentRole: Role = Role.Admin;
console.log(currentRole); // 1


Hoặc bạn có thể gán tên cụ thể:

enum Status {
  Success = "SUCCESS",
  Fail = "FAIL",
  Pending = "PENDING"
}

9. Class và OOP trong TypeScript 🧱

TypeScript hỗ trợ lập trình hướng đối tượng cực tốt.

Ví dụ:

class Animal {
  name: string;

  constructor(name: string) {
    this.name = name;
  }

  speak(): void {
    console.log(`${this.name} kêu...`);
  }
}

class Dog extends Animal {
  bark(): void {
    console.log(`${this.name} sủa gâu gâu`);
  }
}

const dog = new Dog("Milu");
dog.speak(); // Milu kêu...
dog.bark();  // Milu sủa gâu gâu


Cũng có thể dùng modifier: public, private, protected để kiểm soát phạm vi truy cập.

10. Generics – Viết code linh hoạt hơn 🔄

Generics giúp bạn viết function hoặc class có thể làm việc với nhiều kiểu dữ liệu.

function identity<T>(value: T): T {
  return value;
}

let num = identity<number>(123);
let str = identity<string>("Hello");


Hoặc với mảng:

function firstItem<T>(arr: T[]): T {
  return arr[0];
}

11. Type Assertion – Khi bạn biết rõ hơn TypeScript 😎

Khi TS không thể suy luận chính xác kiểu, bạn có thể “gợi ý” cho nó:

let input = document.querySelector("input#username") as HTMLInputElement;
input.value = "trongbui";


Nếu không khai báo, input sẽ chỉ được hiểu là Element, và không có thuộc tính .value.

12. Làm việc với Module và Import/Export 📦

TypeScript hỗ trợ cú pháp module hiện đại:

math.ts

export function add(a: number, b: number): number {
  return a + b;
}


app.ts

import { add } from "./math";

console.log(add(10, 20));


Khi biên dịch, TS sẽ xuất ra JavaScript tương ứng (thường theo chuẩn ES6 hoặc CommonJS tùy cấu hình).

13. File tsconfig.json – Trái tim của dự án TypeScript ❤️

Khi bạn chạy tsc --init, TS tạo file cấu hình:

{
  "compilerOptions": {
    "target": "ES6",
    "module": "ES6",
    "strict": true,
    "outDir": "./dist",
    "rootDir": "./src"
  }
}


File này cho phép bạn:

Xác định thư mục đầu vào / đầu ra.

Bật kiểm tra chặt chẽ (strict).

Cấu hình phiên bản JS đầu ra (target).

14. Tích hợp TypeScript với React hoặc Vue ⚛️

Đây là phần mà TypeScript thực sự tỏa sáng.

🔷 Với React:

Khi bạn tạo project bằng Vite:

npm create vite@latest my-react-app -- --template react-ts


TS giúp bạn kiểm tra props, event, state:

interface Props {
  name: string;
}

const Hello: React.FC<Props> = ({ name }) => {
  return <h1>Xin chào {name}</h1>;
};

🟢 Với Vue:

Vue 3 hỗ trợ TS cực tốt:

npm create vite@latest my-vue-app -- --template vue-ts


Trong .vue file:

<script lang="ts">
export default {
  data() {
    return { count: 0 }
  }
}
</script>

15. Một số lỗi phổ biến khi mới học TypeScript ⚠️
Tình huống	Nguyên nhân
“Cannot find name…”	Quên khai báo biến hoặc import
“Type ‘string’ is not assignable to type ‘number’”	Sai kiểu dữ liệu
“Property does not exist on type…”	Truy cập thuộc tính không được khai báo
Không chạy được tsc	Chưa cài TypeScript hoặc cấu hình sai tsconfig.json
16. Lời khuyên khi bắt đầu học TypeScript 🌱

Đừng sợ type! — ban đầu có thể hơi rối, nhưng sau vài ngày bạn sẽ thấy nó giúp bạn tiết kiệm thời gian debug gấp 10 lần.

Học song song với dự án nhỏ: chuyển một file .js sang .ts để cảm nhận sự khác biệt.

Đọc lỗi TypeScript cẩn thận — nó rất thông minh, thường gợi ý cách sửa.

Kết hợp với framework bạn đang dùng: React, Vue, hoặc Node.

Đọc tài liệu chính thức: https://www.typescriptlang.org/

🏁 Kết luận

TypeScript không chỉ là “JS có type” — nó là một cách tư duy mới khi viết code: an toàn, rõ ràng, và dễ mở rộng.
Khi dự án của bạn lớn hơn, TypeScript sẽ là “người gác cổng” giúp ngăn lỗi nhỏ trở thành thảm họa.

💬 “JavaScript khiến bạn code nhanh hơn, TypeScript khiến bạn code thông minh hơn.”